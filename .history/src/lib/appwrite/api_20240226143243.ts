import { INewUser } from "@/types";

export async function createUserAccount(use: INewUser) {
    try {
        const newAccount()
    } catch (error) {
        console.log(error);
        return error;
    }
}
